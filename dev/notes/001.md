1. Mark remaining work-items with current status (DONE / IN PROGRESS / TODO) across dev/work-items.
2. Tighten VS Code extension behavior (refresh only when preview active, smarter diagnostics ranges).
3. Spec/README polish pass (document known limitations + versioning rules).
4. CI wiring (GitHub Action + JSON report format).