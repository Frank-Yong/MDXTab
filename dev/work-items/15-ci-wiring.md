# Work Item: CI wiring

## Description
Automate validation and tests in CI to keep the format deterministic.

## Sub-items
- Add workflow to run lint/tests and golden vectors.
- Add `mdxtab validate` step over repo docs/examples.
- Fail PRs on diagnostics; publish reports as artifacts if useful.
