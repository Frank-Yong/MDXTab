import type { FrontmatterDocument } from "./types.js";

export function parseFrontmatter(raw: string): FrontmatterDocument {
  // TODO: Implement YAML parsing with strict schema checks per formal spec.
  throw new Error("parseFrontmatter not implemented");
}
