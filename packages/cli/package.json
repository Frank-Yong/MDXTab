{
  "name": "@mdxtab/cli",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "bin": {
    "mdxtab": "dist/bin.js"
  },
  "main": "dist/cli.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run -w @mdxtab/core build --if-present && vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@mdxtab/core": "1.0.0"
  }
}
