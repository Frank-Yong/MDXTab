{
  "name": "mdxtab",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "test": "npm run -ws test"
  },
  "devDependencies": {
    "@types/node": "^22.2.0",
    "typescript": "^5.5.4",
    "vitest": "^1.6.0"
  }
}
