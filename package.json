{
  "name": "mdxtab",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @mdxtab/core build",
    "test": "npm run -w @mdxtab/core test"
  },
  "devDependencies": {
    "@types/node": "^22.2.0",
    "typescript": "^5.5.4",
    "vitest": "^1.6.0"
  }
}
